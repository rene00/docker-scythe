#!/bin/sh
# this script is not called yet though I'm committing it as a an example
# of how vnc can be setup with a password. This is untested also.

if test -z "${VNC_PASSWD}"; then
    VNC_PASSWD=1234
fi

mkdir /root/.vnc
x11vnc -storepasswd ${VNC_PASSWD} ~/.vnc/passwd
